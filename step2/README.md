# Making a dynamic proxy

We can use a proxy to help us handle things like transactions. This avoids code duplication. See the `ProxyHandler` class, which implements the `InvocationHandler` interface.
